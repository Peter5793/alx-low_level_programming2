#include <stdio.h>

/**
 * main - Entry point for Alphabet
 *
 * Return: ALways 0 (Success)
 */

int main(void)
{
char str;
for (str = 'a' ; str <= 'z' ; str++)
{
putchar(str);
}
putchar('\n');
return (0);
}
